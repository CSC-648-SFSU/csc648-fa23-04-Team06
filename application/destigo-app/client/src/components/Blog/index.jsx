import { useState } from "react";

const Blog = () =>{
    const [setId, setToId] = useState("");
    const [message, setMessage] = useState([]);
    const [conversation, setConversations] = useState([]);

    const handleSendMessage = async (e) => {
        c
    }


}